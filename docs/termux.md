# Termux Quick Guide

1. Run `scripts/setup_termux.sh`
2. Install backend/frontend dependencies
3. Build llama.cpp for ARM64
4. Use quantized GGUF models (Q4_K_M or smaller on mobile)
5. Start backend and inference with low-memory defaults
